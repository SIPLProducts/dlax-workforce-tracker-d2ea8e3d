# Weekly Report signatures: remove boxes

Keep only the four labels with blank signature space — no bordered box or dividers.

## Screen (Reports > Weekly)
- Remove the outer border/rounded box and the column divider borders from the signature row.
- Keep a plain 4-column grid: label text on top, blank ruled line below for signing.

## PDF download
- Remove the rectangle outline and vertical divider lines in the signature band.
- Keep the four labels and the thin signature rule under each, evenly spaced across the page width.
- Keep the existing page-break check so the block never gets clipped.

Nothing else in the report (table, totals, header, logo, filters) changes.

Update `src/routes/masters.departments.tsx` — in the Categories column, render each badge as `CAT-### — Name` instead of just `Name`, using the `category_code` already loaded with `allCategories`. Fallback to just the name if no code.